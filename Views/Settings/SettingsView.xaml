<Window x:Class="UMLGenerator.Views.Settings.SettingsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UMLGenerator.Views.Settings"
        xmlns:vm="clr-namespace:UMLGenerator.ViewModels.Settings"
        xmlns:v="clr-namespace:UMLGenerator.Views.Settings"
        mc:Ignorable="d"
        Title="Settings" Height="800" Width="800" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.Resources>
            <DataTemplate DataType="{x:Type vm:GithubSettingsViewModel}">
                <v:GithubSettingsView DataContext="{Binding }"/>
            </DataTemplate>
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border BorderBrush="Black" BorderThickness="1" Margin="10,0,0,0">
            <StackPanel Width="150">
                <Button Content="Github API" Height="30" Background="Transparent"/>
            </StackPanel>
        </Border>
        <ContentControl Content="{Binding SelectedViewModel}" Grid.Column="1"/>
    </Grid>
</Window>
